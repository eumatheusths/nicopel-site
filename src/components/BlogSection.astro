---
// src/components/BlogSection.astro
import { blogPosts } from '../data/blogPosts';

// ALTERAÇÃO AQUI:
// Pega os 3 últimos posts do array (os novos) e inverte para mostrar o mais recente primeiro
const recentPosts = blogPosts.slice(-3).reverse();
---

<section class="py-24 bg-white relative overflow-hidden" id="blog">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
      <div class="max-w-2xl">
        <h2 class="text-4xl font-extrabold text-gray-900 tracking-tight">
          Dicas para alavancar <br />
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#8bb300] to-[#5a7d00]">seu negócio.</span>
        </h2>
      </div>
      <a href="/blog" class="hidden md:inline-flex items-center gap-2 px-6 py-3 bg-white border border-gray-200 rounded-full font-bold text-gray-700 hover:bg-gray-50 hover:text-[#5a7d00] transition-all">
        Ver todos os artigos
      </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {recentPosts.map((post) => (
            <article class="group cursor-pointer flex flex-col h-full bg-gray-50 rounded-2xl overflow-hidden border border-gray-100 hover:shadow-lg transition-all duration-300">
                <a href={`/blog/${post.slug}`} class="relative h-64 overflow-hidden block">
                    <img 
                      src={post.image} 
                      alt={post.title} 
                      class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
                    />
                    <div class="absolute inset-0 bg-black/10 group-hover:bg-transparent transition-all"></div>
                    <div class="absolute top-4 left-4 bg-white/90 backdrop-blur-sm px-3 py-1 rounded-lg text-xs font-bold text-gray-900 z-10">
                        {post.category}
                    </div>
                </a>
                <div class="flex-1 flex flex-col p-6">
                    <div class="text-xs text-gray-400 font-medium mb-2">{post.date}</div>
                    <a href={`/blog/${post.slug}`}>
                        <h3 class="text-xl font-bold text-gray-900 mb-3 leading-snug group-hover:text-[#8bb300] transition-colors">
                            {post.title}
                        </h3>
                    </a>
                    <a href={`/blog/${post.slug}`} class="mt-auto inline-flex items-center text-sm font-bold text-gray-900 hover:text-[#B8E900] transition-colors">
                        Ler mais
                    </a>
                </div>
            </article>
        ))}
    </div>
    
    <div class="mt-8 text-center md:hidden">
        <a href="/blog" class="inline-flex items-center gap-2 px-6 py-3 bg-white border border-gray-200 rounded-full font-bold text-gray-700 hover:bg-gray-50 hover:text-[#5a7d00] transition-all">
            Ver todos os artigos
        </a>
    </div>

  </div>
</section>